apiVersion: v1
kind: Service
metadata:
  name: personal-ai-assistant
spec:
  selector:
    app: personal-ai-assistant
  type: LoadBalancer  # exposes it publicly
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
